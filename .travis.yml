language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t codebloxltd/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true codebloxltd/docker-react npm run test -- --coverage